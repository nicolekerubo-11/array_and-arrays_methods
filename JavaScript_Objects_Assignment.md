# JavaScript Objects Assignment

## Topic: Objects + Arrays + Functions

### Instructions

-   Solve using JavaScript only
-   Write complete working code
-   Do not copy from the internet
-   Use proper variable names
-   Functions must be reusable

------------------------------------------------------------------------

## 1️⃣ Object Modification

Create an object representing a user with properties: name, age, and
role.

Then: - Update the role - Add a new property called active - Remove the
age property

------------------------------------------------------------------------

## 2️⃣ Object Iteration

Create an object representing a product (name, price, brand).

Write code that displays all keys and their values in a readable format.

------------------------------------------------------------------------

## 3️⃣ Convert Object to Array

Create an object that stores subject names as keys and scores as values.

Transform it into an array of objects where each object contains: -
subject - score

------------------------------------------------------------------------

## 4️⃣ Sum Object Values

Create an object representing monthly expenses.

Write a function that calculates the total expense.

------------------------------------------------------------------------

## 5️⃣ Find Highest Value in Object

Create an object containing categories and numeric ratings.

Write a function that returns the category with the highest rating.

------------------------------------------------------------------------

## 6️⃣ Nested Object Manipulation

Create an object representing a student with a nested address object.

Write code to: - Access one nested property - Update one nested
property - Add a new nested property

------------------------------------------------------------------------

## 7️⃣ Count Object Properties

Write a function that receives an object and returns the number of
properties it contains.

------------------------------------------------------------------------

## 8️⃣ Merge Two Objects

Create two separate objects representing user information and user
preferences.

Write a function that merges them into one single object.

------------------------------------------------------------------------

## 9️⃣ Remove Property From Object

Write a function that removes a specified property from an object
without modifying the original object.


-----------------------------------------------------------------------

## 🔟 Validate Object Structure

Write a function that checks whether an object: - Has a name property
(string) - Has an age property (number) - Has an email property
containing "@"

Return true or false.

------------------------------------------------------------------------

## 1️⃣1️⃣ Group Array of Objects

Create an array of objects representing people with name and age.

Write a function that groups them by age.

------------------------------------------------------------------------

## 1️⃣2️⃣ Remove Duplicate Objects

Create an array of objects with duplicate IDs.

Write a function that removes duplicates based on ID.

------------------------------------------------------------------------

## 1️⃣3️⃣ Deep Clone Object

Write a function that deeply clones an object that contains nested
objects and arrays.

------------------------------------------------------------------------

## 1️⃣4️⃣ Object Property Counter by Condition

Write a function that counts how many properties in an object have
numeric values greater than 50.

------------------------------------------------------------------------

## 1️⃣5️⃣ Build a Mini Student Report System

Create an array of student objects. Each student should have: - name -
an array of scores

Write a function that returns a new array containing: - name - average
score - grade (A, B, C, D, F)
